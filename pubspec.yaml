name: automobile_management
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.1 <3.0.0"

dependencies:
  cloud_firestore: ^4.4.5
  cupertino_icons: ^1.0.5
  firebase_auth: ^4.3.0
  firebase_core: ^2.8.0
  firebase_storage:
  flutter:
    sdk: flutter
  get_it: ^7.2.0
  provider:
  image_picker:
  fluttertoast:
  google_fonts:
  cached_network_image: ^3.2.3
  get_storage: ^2.1.1
  geolocator: ^9.0.2
  flutter_riverpod: ^2.3.4
  swipe_to:
  path_provider:
  permission_handler:
  firebase_messaging:
  flutter_local_notifications: 13.0.0
  file_picker:
  carousel_slider:

  intl: ^0.17.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
